node_modules
dist
dist-embed
dist-ssr
*.local
.DS_Store

# secrets / local env (CONTRIB_ADMIN_TOKEN, blob/KV/turnstile keys)
.env
.env.*
!.env.example

# test artifacts
test-results
playwright-report
playwright/.cache
coverage

# onnxruntime-web runtime — copied into web/public/ort by web's postinstall (13 MB)
web/public/ort/

# Korg preset content — not licensed for redistribution; kept locally for tests
*.mnlgxdprog
*.mnlgxdlib

# python training project
__pycache__/
*.py[cod]
.venv/
training/checkpoints/
*.pt

# generated datasets (audio + manifests) — large, regenerable
training/data/surge/
training/data/xd/
training/data/hard_cases/
training/data/contrib/
training/data/contrib_accepted/
training/eval/set/
training/.daemon.pid
training/.daemon.log
training/.nightly.log
training/.nightly.lock

# local-only working files (audio targets, patch libraries, scratch notes), kept out of git
/_local/
.vercel
.env*
